'use strict';

define(function(require, exports, module) {

    // We have to use pre-built version because controllers are loaded asynchronously
    var RC = require('PATH_TO_LIB/browser');

    console.log('RC.Crypto:', RC.Crypto);

});